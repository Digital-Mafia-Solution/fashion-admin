// NOTE: sidebar core/provider implementation moved to
// `sidebar-core.ts` and `sidebar-provider.tsx` to satisfy
// react-refresh's rule that a file exporting components must
// not also export non-component values. This file is intentionally
// left as a no-op module to avoid accidental imports.

export {};
